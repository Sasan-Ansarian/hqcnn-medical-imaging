experiment:
  name: "dermamnist_resnet18_baseline_v1"
  seed: 42
  output_dir: "outputs"

dataset:
  name: "dermamnist"
  root: "data/medmnist"

training:
  device: "cuda"
  epochs: 5
  batch_size: 128
  lr: 0.0005
  weight_decay: 0.01
  num_workers: 2

model:
  name: "resnet18_small"
